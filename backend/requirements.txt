# ── Web Framework ─────────────────────────────────────────────────────────────
fastapi==0.133.1
starlette==1.3.1
uvicorn[standard]==0.30.1
gunicorn==22.0.0

# ── Data Validation & Settings ────────────────────────────────────────────────
pydantic==2.7.4
pydantic-settings==2.3.1
email-validator==2.1.1

# ── Database ──────────────────────────────────────────────────────────────────
sqlalchemy==2.0.31
alembic==1.13.2
asyncpg==0.29.0          # async PostgreSQL driver
aiosqlite==0.20.0        # async SQLite driver used by integration tests
psycopg2-binary==2.9.9   # sync driver (Alembic migrations)

# ── Caching & Task Queue ──────────────────────────────────────────────────────
redis==5.0.7
celery==5.4.0

# ── Authentication & Security ─────────────────────────────────────────────────
PyJWT==2.13.0
cryptography==50.0.0
# Apple App Attest verifier and its security-sensitive direct dependency set.
# These are pinned together because provider verification must not drift during
# an otherwise unrelated backend rebuild.
pyattest==1.0.5
cbor2==6.1.4
asn1crypto==1.5.1
pyhanko-certvalidator==0.31.4
oscrypto==1.3.0
uritools==6.1.3
# Application JWTs use the PyJWT + cryptography stack pinned above. pyattest's
# metadata still pulls python-jose/ecdsa transitively even though its verifier
# source does not import them; the exact non-signing exception is documented
# and enforced in CI instead of pretending the dependency is absent.
# The application imports bcrypt directly; avoid carrying the unused and
# unmaintained Passlib compatibility layer into the production dependency set.
bcrypt==5.0.0
python-multipart==0.0.32  # file uploads

# ── HTTP Client (for webhooks, cloud APIs) ────────────────────────────────────
httpx==0.27.0
google-auth==2.56.3       # Application Default Credentials for Play Integrity REST verification

# ── Storage ───────────────────────────────────────────────────────────────────
boto3==1.34.131          # S3-compatible (AWS, MinIO, etc.)
botocore==1.34.131

# ── Image Processing ──────────────────────────────────────────────────────────
opencv-python-headless==4.10.0.84
Pillow==12.3.0
pillow-heif==1.4.0
qrcode==8.2
numpy==1.26.4

# ── OCR Engines (pluggable — only what's available will be loaded) ────────────
pytesseract==0.3.10

# ── MRZ Parsing ───────────────────────────────────────────────────────────────
passporteye==2.2.1

# ── Excel Export ──────────────────────────────────────────────────────────────
openpyxl==3.1.4
pycountry==26.2.16

pypdf==6.15.0
pypdfium2==5.12.1

# ── Observability ─────────────────────────────────────────────────────────────
structlog==24.2.0
sentry-sdk[fastapi]==2.66.0

# ── Utilities ─────────────────────────────────────────────────────────────────
python-dateutil==2.9.0
tzdata==2026.3           # explicit IANA database for ZoneInfo on minimal/Windows runtimes
shortuuid==1.0.13
tenacity==8.3.0          # retry logic
